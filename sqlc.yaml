version: "2"
sql:
  - schema: "migrations/"
    queries: "internal/expenses/adapters/db/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/expenses/adapters/db"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true # Essential for hexagonal architecture & mocking
        emit_exact_table_names: false